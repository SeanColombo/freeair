package com.seancolombo.freeair.airquality

/** A plain RGB color (0-255 per channel), kept framework-agnostic so domain code stays pure Kotlin. */
data class RgbColor(val red: Int, val green: Int, val blue: Int)
